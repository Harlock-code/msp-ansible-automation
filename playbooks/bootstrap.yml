---
- name: Bootstrap Docker Host
  hosts: all
  become: true

  roles:
    - role: common
      tags: common

    - role: security
      tags: security

    - role: docker_engine
      tags: docker_engine

    - role: monitoring
      tags: monitoring

    - role: docker_stack
      tags: docker_stack

